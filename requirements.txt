# This file is for CircleCI until I can figure
# out how to get it to do "pip install -e"
# The "pygments" version is not quite right
# And the xdis version is missing
columnize >= 0.3.10
pyficache >= 2.3.0,
Pygments-rocky-hacked == 2.0
spark_parser
tracer >= 0.3.2
flake8
uncompyle6 >= 3.9.2
# Get these from github
# pyficache >= 2.1.0, < 2.2.0
# xdis >= 4.7.0
